<h1>All Cocktails</h1>

<ul>
  <% @cocktails.each do |cocktail| %>
<li><%= cocktail.name %> <%= link_to 'Show', cocktail_path(cocktail) %></li>
  <% end %>
</ul>

<%= link_to 'Create a new cocktail', new_cocktail_path %>

<%= cl_image_tag("bz522m3pubp0fddgzwqz",
      width: 400, height: 300, crop: :fill) %>
